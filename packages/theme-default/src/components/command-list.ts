import { css } from "lit";

export const commandListStyles = css`
  dui-scroll-area {
    max-height: calc(var(--space-5) * 15);
    height: auto;
  }

  .List {
    padding: var(--space-1) 0;
  }
`;
